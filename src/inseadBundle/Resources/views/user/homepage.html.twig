{% extends 'base.html.twig' %}

{% block body %}

    <div class="barre"></div>

    <div class="parallax-container col s12">
        <div class="container">
            <div class="row logo">
                <img class="col l4 m6 s12 offset-l4 offset-m3 center-align" src="{{ asset('bundles/insead/img/Soloista Logo.png') }}">
            </div>
        </div>
        <div class="parallax"><img src="{{ asset('bundles/insead/img/insead-image-2.jpg') }}"></div>
    </div>

    <div class="row btn_home">
        <a class="waves-effect waves-red btn btn_custom btn_custom_home col l2 s3 offset-s4 offset-l5 flow-text" href="{{ path('insead_menu') }}">Next</a>
    </div>

    <div class="container">
        <div class="row">
            <p class="col s12 center-align title flow-text">Whether you are traveling or planning an upcoming trip, login or register to connect with other women solo travelers that have the same interests as you.</p>
        </div>
        <div class="row">
            <p class="col s12 center-align title flow-text">We will give you recommendations of places to go and things to do as well as connect you with Localistas who can give you all the insider tips and great deals.</p>
        </div>
    </div>
    <div class="row btn_home">
        <a class="waves-effect waves-red btn btn_custom col l2 s4 offset-s1 offset-l3 flow-text" href="{{ path('fos_user_security_login') }}">LogIn</a>
        <a class="waves-effect waves-red btn btn_custom col l2 s4 offset-s1 offset-l2 flow-text" href="{{ path('fos_user_registration_register') }}">Register</a>
    </div>

    <div class="parallax-container">
        <div class="parallax"><img src="{{ asset('bundles/insead/img/insead-image.jpg') }}"></div>
    </div>

    <div class="barre"></div>

{% endblock %}